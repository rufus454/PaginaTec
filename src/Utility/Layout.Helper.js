/**
 * Función para generar el título de la página
 * @param {string} title Título de la página
 * @returns {string}
 */
export const generateTitle = (title) => {
  return `${title} - TecNM Campus Delicias`;
};
